//on creation of a template
var formItems = document.getElementById( 'mainForm' );
var allFormControls = formItems.querySelectorAll("*");
console.log(formItems);

//get elements on the form

//transverse form DOM and prefix ID tags to make unique. 

// Add prefix's to the id tags in the form template so no conflicts occur

//get all id's in form template


// document.querySelectorAll(CSS selectors)

// var ids = []; 
// $("*").each(function() { 
//     if (this.id) { 
//         ids.push(this.id); 
//     } 
// }); 


